<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />

    <!--
        ELIMINADO (Issue 3.2):
          - WRITE_EXTERNAL_STORAGE con maxSdkVersion=32 → solo útil < API 23, redundante ahora.
          - MANAGE_EXTERNAL_STORAGE → acceso a TODO el almacenamiento del usuario;
            Google Play lo rechaza salvo en apps tipo file-manager/antivirus.
            Migra a Scoped Storage con MediaStore.Downloads en PathUtils.

        ELIMINADO (Issue 4.2):
          - No se incluye soporte para bypass de SSL. Si necesitas debugar SSL
            en desarrollo, usa res/xml/network_security_config.xml con dominios
            específicos y solo en debug-overrides.
    -->

    <application
        android:name=".MyApplication"
        android:allowBackup="false"
        android:hasFragileUserData="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">

        <!--
            ELIMINADO (Issue 3.2):
              android:requestLegacyExternalStorage="true"
            Es ignorado en Android 11+ (API 30+) y no aporta nada con targetSdk=37.
        -->

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.MyApplication">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />
        </activity>

        <activity
            android:name=".ui.ShareReceiverActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:launchMode="singleTask"
            android:theme="@style/Theme.Transparent">
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.fabian.downloader.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <service
            android:name=".services.DownloadForegroundService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <receiver
            android:name=".receivers.DownloadActionReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="com.fabian.downloader.ACTION_PAUSE" />
                <action android:name="com.fabian.downloader.ACTION_OPEN" />
                <action android:name="com.fabian.downloader.ACTION_SHARE" />
                <action android:name="com.fabian.downloader.ACTION_RETRY" />
            </intent-filter>
        </receiver>
    </application>

</manifest>
